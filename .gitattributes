# Chỉ các file vượt giới hạn cứng 100 MB/file của GitHub mới đưa vào Git LFS,
# để tiêu tốn ít quota LFS nhất có thể. Các file .joblib (~28 MB) đi qua git thường.
runs_v05_revised/**/test_predictions_long.csv filter=lfs diff=lfs merge=lfs -text
